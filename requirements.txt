from sovereign_master.security.authentication import TokenAuthenticator


def require_admin(token: str | None, username: str | None, secret: str | None = None) -> bool:
    authenticator = TokenAuthenticator(secret=secret)
    return authenticator.verify_token(token, username)
